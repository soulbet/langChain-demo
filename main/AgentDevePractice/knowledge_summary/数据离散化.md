## 1. 核心定义
离散化是将连续特征转换为离散特征的过程，通常用于数据预处理，使得模型能够更好地处理和学习。

## 2. 数学原理/公式
- **等宽分箱（Uniform）**：将数据的取值范围等分，每个箱的宽度相同。
- **等频分箱（Quantile）**：根据数据的分位数进行分箱，每个箱内的样本数量大致相等。
- **聚类分箱（K-Means）**：使用 K-Means 聚类算法，在数据密集的地方切得密一点，在数据稀疏的地方切得疏一点。

## 3. 关键要点
- **等宽分箱**：适用于数据分布均匀且没有异常值的情况，但容易受到长尾和离群值的影响。
- **等频分箱**：适用于大多数工业界场景，特别是带有长尾分布的数据，不受异常值影响。
- **聚类分箱**：能够贴合数据本身的空间分布，但计算量大，速度慢，且结果具有随机性。

## 4. 代码示例 (Python)
```python
import numpy as np
from sklearn.preprocessing import KBinsDiscretizer

# 假设有 6 个人的年龄（注意长尾：最后一个人是 100 岁）
X = np.array([[20], [22], [25], [28], [30], [100]])

# 等宽分箱
est_wide = KBinsDiscretizer(n_bins=3, encode='ordinal', strategy='uniform')
X_wide = est_wide.fit_transform(X)
print("等宽分箱边界:", est_wide.bin_edges_)
print("等宽分箱结果:\n", X_wide.flatten())

# 等频分箱
est_freq = KBinsDiscretizer(n_bins=3, encode='ordinal', strategy='quantile')
X_freq = est_freq.fit_transform(X)
print("等频分箱边界:", est_freq.bin_edges_)
print("等频分箱结果:\n", X_freq.flatten())

# 聚类分箱
est_kmeans = KBinsDiscretizer(n_bins=3, encode='ordinal', strategy='kmeans')
X_kmeans = est_kmeans.fit_transform(X)
print("聚类分箱结果:\n", X_kmeans.flatten())
```

## 5. 面试常见问题
1. 什么是离散化？它在数据预处理中的作用是什么？
2. 等宽分箱和等频分箱的主要区别是什么？
3. 聚类分箱的优缺点是什么？

## 6. 优缺点
- **优点**：
  - 等频分箱：不受异常值影响，适用于长尾分布数据。
  - 聚类分箱：贴合数据本身的空间分布。
- **缺点**：
  - 等宽分箱：容易受到长尾和离群值的影响。
  - 聚类分箱：计算量大，速度慢，且结果具有随机性。

## 7. 常用的使用场景
- 离散化常用于处理带有长尾分布的数据，如收入、交易额等。
- 在数据分布均匀且没有异常值的情况下，等宽分箱也可以使用。