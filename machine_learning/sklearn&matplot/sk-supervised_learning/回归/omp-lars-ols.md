
## 选 LARS
- 特征数远大于样本 p≫n；
- 需要画 Lasso 正则化路径、不用暴力网格搜 λ；
- 想自动给特征做重要性排序、筛选变量；
- 特征之间高度相关（多重共线性）。
## 选 OMP
- 压缩感知、图像修复、MRI 重建、信号去噪；
- 明确知道模型只要K 个非零特征；
- 过完备字典稀疏分解，不需要正则路径。
## 选 OLS
- 样本远多于特征，矩阵满秩；
- 不需要稀疏、不需要正则路径；
- 只做简单线性拟合、基准建模。