# 运行自定义函数
- RunnableLambda
  - 使用 RunnableLambda 构造函数显式包装我们的自定义逻辑
- @chain
  - 通过添加 @chain 装饰器将任意函数转换为链
- 链中的自动强制转换
  -  `prompt | model | (lambda x: x.content[:5])`  函数接受模型的输出并返回前五个字母
## 传递运行元数据
> 核心作用是把 invoke 方法中指定的配置（如回调、标签等），原封不动地穿过程序的层层调用，准确无误地传递给下游需要它的组件